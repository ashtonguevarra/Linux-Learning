filename="/home/ashton/bin/scriptThree"
if [ -x "$filename" ] ; then
	echo "$filename is executable"
else
	echo "The file is not executable"
fi

if [ -w "$filename" ] ; then
	echo "$filename is writable"
else 
	echo "The file is not writable"
fi
